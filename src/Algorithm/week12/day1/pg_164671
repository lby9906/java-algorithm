SELECT CONCAT('/home/grep/src/', b.BOARD_ID, '/', f.FILE_ID, f.FILE_NAME, f.FILE_EXT) AS 'FILE_PATH' FROM USED_GOODS_BOARD b
INNER JOIN USED_GOODS_FILE f ON f.BOARD_ID = b.BOARD_ID
WHERE b.VIEWS IN (
    SELECT MAX(VIEWS)
    FROM USED_GOODS_BOARD
)
ORDER BY f.FILE_ID DESC;