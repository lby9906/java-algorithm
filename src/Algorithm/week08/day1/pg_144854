SELECT b.BOOK_ID, a.AUTHOR_NAME, DATE_FORMAT(b.PUBLISHED_DATE, '%Y-%m-%d') AS PUBLISHED_DATE FROM BOOK b INNER JOIN AUTHOR a ON a.AUTHOR_ID = b.AUTHOR_ID WHERE CATEGORY LIKE '%경제%'
ORDER BY PUBLISHED_DATE ASC;